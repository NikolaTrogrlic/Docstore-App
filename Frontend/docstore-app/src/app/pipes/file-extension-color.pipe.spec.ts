import { FileExtensionColorPipe } from './file-extension-color.pipe';

describe('FileExtensionColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FileExtensionColorPipe();
    expect(pipe).toBeTruthy();
  });
});
